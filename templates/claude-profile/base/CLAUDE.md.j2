# {{ project_name }} — AI Assistant

> Auto-generated by CodeFoundry ({{ archetype }} archetype)

---

## Role

Expert {{ archetype }} developer. Primary language: {{ language }}.

---

## P0: Session Start

1. Read SESSION.md for current context
2. Check TASKS.md for active tasks

---

## P1: Constraints

### Environment

```
local     → {% if docker_enabled %}Docker available{% else %}No Docker{% endif %}
production → Deploy via git push
```

### Tool Selection

| Task | Tool |
|------|------|
| Edit files | `Edit` tool |
| Execute commands | `Bash` tool |
| Search content | `Grep` tool |
| Find files | `Glob` tool |

**NEVER use Bash for file editing.**

### GitOps

**Git = Single Source of Truth.** All deployments via `git push`.

---

## P2: Communication

| Context | Language |
|---------|----------|
| User dialogue | Russian |
| Code/docs | English |

---

## Agents

{% for agent in agents -%}
- [{{ agent }}](.claude/agents/{{ agent }}.md)
{% endfor %}
---

## Quick Reference

| Intent | Action |
|--------|--------|
| Check status | TASKS.md |
| Deploy | git commit → push |
| Health check | `/health` |
| Run tests | `make test` |

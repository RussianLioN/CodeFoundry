> [🏠 Главная](../README.md) → [📂 Документация](INDEX.md) → **🔐 GitHub Actions Secrets Setup**

---

# Настройка GitHub Secrets для Remote Sync

> **Workflow:** `.github/workflows/remote-sync.yml`
> **Статус:** ✅ SSH доступ настроен, требуется добавить GitHub Secrets

---

## 📋 Текущее состояние

**Основной CI pipeline:** ✅ Прходит успешно
**OpenClaw Remote Sync:** ❌ Failed — отсутствуют GitHub Secrets
**SSH доступ к серверу:** ✅ Уже настроен (`ainetic.tech`)

**Ошибка:**
```
error: missing server host
REMOTE_HOST: (пусто)
REMOTE_USER: (пусто)
```

---

## 🔑 Требуемые GitHub Secrets

### Реальные значения (CodeFoundry)

| Secret | Значение | Источник |
|--------|----------|----------|
| `REMOTE_HOST` | `45.148.117.156` или `ainetic.tech` | SSH config |
| `REMOTE_USER` | `root` | SSH config |
| `SSH_PRIVATE_KEY` | содержимое `~/.ssh/id_n8n_servers` | Существующий ключ |
| `SSH_PORT` | `22` | Стандартный порт |
| `REMOTE_PATH` | `/opt/openclaw` | ✅ Директория существует |

### Опциональные

| Secret | Описание | Значение по умолчанию |
|--------|----------|----------------------|
| `TELEGRAM_BOT_TOKEN` | Токен бота для уведомлений | — |
| `TELEGRAM_CHAT_ID` | Chat ID для уведомлений | — |

---

## 🚀 Быстрая настройка (с существующим SSH ключом)

### Предварительная проверка

```bash
# 1. Проверить что SSH доступ работает
ssh ainetic.tech "echo '✅ Connection OK' && whoami"

# 2. Проверить что публичный ключ на сервере
ssh-keygen -y -f ~/.ssh/id_n8n_servers | ssh ainetic.tech "grep -q '$(cat)' ~/.ssh/authorized_keys" && echo '✅ Key already on server'

# 3. Проверить что директория OpenClaw существует
ssh ainetic.tech "ls -ld /opt/openclaw"
```

**Ожидаемый результат:**
```
✅ Connection OK
root
✅ Key already on server
drwxr-xr-x 22 root root 4096 Feb 2 19:27 /opt/openclaw
```

### Автоматическая настройка через CLI ✅ РЕКОМЕНДУЕТСЯ

Используйте готовый скрипт для добавления secrets:

```bash
# Запустить из корня репозитория
./scripts/setup-github-secrets.sh

# Или проверить что будет сделано (dry-run)
./scripts/setup-github-secrets.sh --dry-run
```

**Скрипт автоматически:**
1. ✅ Проверит prerequisites (gh CLI, SSH доступ)
2. ✅ Прочитает существующий SSH ключ `~/.ssh/id_n8n_servers`
3. ✅ Добавит все необходимые secrets в GitHub
4. ✅ Проверит результат
5. ✅ Покажет инструкции для тестирования workflow

**Требования:**
- Установленный `gh` CLI: `brew install gh`
- Аутентификация: `gh auth login`
- Существующий SSH ключ: `~/.ssh/id_n8n_servers`

---

## 🔧 Ручная настройка

Если автоматический скрипт не сработает:

### Шаг 1: Получить SSH ключ

```bash
# macOS — копирует в буфер обмена
cat ~/.ssh/id_n8n_servers | pbcopy

# Linux — копирует в буфер обмена
cat ~/.ssh/id_n8n_servers | xclip -sel clip

# Вывод для копирования вручную (безопасный способ — только часть)
cat ~/.ssh/id_n8n_servers | head -5
```

**Важно:** Ключ должен включать BEGIN/END строки:
```
-----BEGIN OPENSSH PRIVATE KEY-----
b3BlbnNzaC1rZXktdjEAAAAABG5vbmUAAAAEbm9uZQAAAAAAAAABAAAAMwAAAAtzc2gtZW
...
-----END OPENSSH PRIVATE KEY-----
```

### Шаг 2: Добавить Secrets в GitHub

1. Откройте: https://github.com/RussianLioN/CodeFoundry/settings/secrets/actions
2. Нажмите "New repository secret"
3. Добавьте каждый secret:

| Name | Secret |
|------|--------|
| `REMOTE_HOST` | `ainetic.tech` |
| `REMOTE_USER` | `root` |
| `SSH_PRIVATE_KEY` | *(вставить из буфера обмена)* |
| `SSH_PORT` | `22` |
| `REMOTE_PATH` | `/opt/openclaw` |

### Шаг 3: Проверка

```bash
# Зафиксировать любое изменение для теста
git commit --allow-empty -m "test: trigger remote sync"

# Push и проверить Actions tab
git push origin main

# Открыть: https://github.com/RussianLioN/CodeFoundry/actions
```

**Ожидаемый результат:**
```
✅ Validate Changes
✅ Sync to Remote Server
✅ Create Backup
```

---

## ⚠️ Важные замечания

### SSH Key Format

**Правильный формат `SSH_PRIVATE_KEY`:**
```
-----BEGIN OPENSSH PRIVATE KEY-----
b3BlbnNzaC1rZXktdjEAAAAABG5vbmUAAAAEbm9uZQAAAAAAAAABAAAAMwAAAAtzc2gtZW
...
-----END OPENSSH PRIVATE KEY-----
```

- **Включайте** `BEGIN` и `END` строки
- **Используйте** `\n` для переноса строк если добавляете через API
- **НЕ используйте** passphrase для ключа (GitHub Actions не сможет ввести пароль)

### Permissions

На сервере проверьте права:
```bash
chmod 700 ~/.ssh
chmod 600 ~/.ssh/authorized_keys
```

---

## 🔧 Альтернативные варианты

### Вариант A: Отключить Remote Sync (если не используется)

Если remote сервер ещё не развёрнут, можно временно отключить workflow:

```bash
# Через CLI
gh workflow disable "OpenClaw Remote Sync"
```

### Вариант B: Использовать существующий SSH ключ

Если у вас уже есть SSH доступ к серверу:

```bash
# Используйте существующий приватный ключ
cat ~/.ssh/id_ed25519 | pbcopy  # скопировать для GitHub Secret
```

---

## ✅ Проверка после настройки

После добавления secrets:

1. Зафиксируйте любое изменение в `main` ветке
2. Откройте Actions tab на GitHub
3. Проверьте что "OpenClaw Remote Sync" workflow прошёл успешно

**Ожидаемый результат:**
```
✅ Sync to remote server
✅ Create backup
```

---

## 📚 Связанная документация

- [Remote Server Architecture](remote-server-architecture.md)
- [GitOps Workflow](gitops-README.md)
- [OpenClaw Installation](../openclaw/install/VDS-SETUP.md)

---

**Последнее обновление:** 2026-02-10
**Статус:** ⚠️ Ожидает настройки GitHub Secrets

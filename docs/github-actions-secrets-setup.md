> [🏠 Главная](../README.md) → [📂 Документация](INDEX.md) → **🔐 GitHub Actions Secrets Setup**

---

# Настройка GitHub Secrets для Remote Sync

> **Workflow:** `.github/workflows/remote-sync.yml`
> **Статус:** ⚠️ Требует настройки GitHub Secrets

---

## 📋 Текущее состояние

**Основной CI pipeline:** ✅ Прходит успешно
**OpenClaw Remote Sync:** ❌ Failed — отсутствуют GitHub Secrets

**Ошибка:**
```
error: missing server host
REMOTE_HOST: (пусто)
REMOTE_USER: (пусто)
```

---

## 🔑 Требуемые GitHub Secrets

### Обязательные (для SSH подключения)

| Secret | Описание | Пример |
|--------|----------|--------|
| `REMOTE_HOST` | IP адрес или домен удалённого сервера | `ainetic.tech` или `123.45.67.89` |
| `REMOTE_USER` | SSH пользователь | `root` или `ubuntu` |
| `SSH_PRIVATE_KEY` | Приватный SSH ключ (полный содержимое файла) | `-----BEGIN OPENSSH PRIVATE KEY-----\n...` |
| `SSH_PORT` | SSH порт (опционально, по умолчанию 22) | `22` |

### Опциональные

| Secret | Описание | Значение по умолчанию |
|--------|----------|----------------------|
| `REMOTE_PATH` | Путь к OpenClaw на сервере | `/opt/openclaw` |
| `TELEGRAM_BOT_TOKEN` | Токен бота для уведомлений | — |
| `TELEGRAM_CHAT_ID` | Chat ID для уведомлений | — |

---

## 🚀 Инструкция по настройке

### Шаг 1: Генерация SSH ключей (если нет)

```bash
# На локальной машине
ssh-keygen -t ed25519 -C "github-actions" -f ~/.ssh/github_actions_key
```

### Шаг 2: Добавление публичного ключа на сервер

```bash
# Копировать публичный ключ на сервер
ssh-copy-id -i ~/.ssh/github_actions_key.pub user@your-server.com

# Или вручную добавить в ~/.ssh/authorized_keys на сервере
cat ~/.ssh/github_actions_key.pub | ssh user@server "cat >> ~/.ssh/authorized_keys"
```

### Шаг 3: Добавление Secrets в GitHub

1. Откройте репозиторий на GitHub
2. Settings → Secrets and variables → Actions
3. Нажмите "New repository secret"
4. Добавьте каждый secret:

```bash
# Получить значения для добавления:

# REMOTE_HOST
echo "ainetic.tech"  # или IP сервера

# REMOTE_USER
echo "root"  # или ваш SSH пользователь

# SSH_PRIVATE_KEY (важно: включая BEGIN/END строки!)
cat ~/.ssh/github_actions_key | pbcopy  # macOS
# или
cat ~/.ssh/github_actions_key | xclip -sel clip  # Linux

# SSH_PORT
echo "22"  # если используется стандартный порт
```

### Шаг 4: Проверка подключения

```bash
# Тест SSH подключения с ключом
ssh -i ~/.ssh/github_actions_key user@server "echo 'Connection successful'"
```

---

## ⚠️ Важные замечания

### SSH Key Format

**Правильный формат `SSH_PRIVATE_KEY`:**
```
-----BEGIN OPENSSH PRIVATE KEY-----
b3BlbnNzaC1rZXktdjEAAAAABG5vbmUAAAAEbm9uZQAAAAAAAAABAAAAMwAAAAtzc2gtZW
...
-----END OPENSSH PRIVATE KEY-----
```

- **Включайте** `BEGIN` и `END` строки
- **Используйте** `\n` для переноса строк если добавляете через API
- **НЕ используйте** passphrase для ключа (GitHub Actions не сможет ввести пароль)

### Permissions

На сервере проверьте права:
```bash
chmod 700 ~/.ssh
chmod 600 ~/.ssh/authorized_keys
```

---

## 🔧 Альтернативные варианты

### Вариант A: Отключить Remote Sync (если не используется)

Если remote сервер ещё не развёрнут, можно временно отключить workflow:

```bash
# Через CLI
gh workflow disable "OpenClaw Remote Sync"
```

### Вариант B: Использовать существующий SSH ключ

Если у вас уже есть SSH доступ к серверу:

```bash
# Используйте существующий приватный ключ
cat ~/.ssh/id_ed25519 | pbcopy  # скопировать для GitHub Secret
```

---

## ✅ Проверка после настройки

После добавления secrets:

1. Зафиксируйте любое изменение в `main` ветке
2. Откройте Actions tab на GitHub
3. Проверьте что "OpenClaw Remote Sync" workflow прошёл успешно

**Ожидаемый результат:**
```
✅ Sync to remote server
✅ Create backup
```

---

## 📚 Связанная документация

- [Remote Server Architecture](remote-server-architecture.md)
- [GitOps Workflow](gitops-README.md)
- [OpenClaw Installation](../openclaw/install/VDS-SETUP.md)

---

**Последнее обновление:** 2026-02-10
**Статус:** ⚠️ Ожидает настройки GitHub Secrets

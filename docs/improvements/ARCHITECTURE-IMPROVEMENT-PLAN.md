# 🏗️ План Улучшения Архитектуры CodeFoundry

> [🏠 Главная](../../README.md) → [📚 Docs](../INDEX.md) → [🔧 Improvements](#) → **Architecture Improvement Plan**

---

## 📋 Метаданные

| Поле | Значение |
|------|----------|
| **Версия** | 1.0.0 |
| **Дата создания** | 2025-02-06 |
| **Статус** | УТВЕРЖДЁН |
| **Автор** | Claude Code (Session analysis) |
| **Экспертный консенсус** | 4.8/10 → Пересмотрен |
| **Связанные сессии** | Session #14 (текущая) |

---

## 📊 Executive Summary

После анализа архитектуры CodeFoundry с привлечением панели из 13 экспертов, были выявлены области для улучшения. Данный документ содержит **пересмотренный план**, исключающий преждевременные оптимизации.

### Исключённые рекомендации (обоснование)

| Рекомендация | Причина исключения |
|--------------|-------------------|
| ~~Абстракция AI providers~~ | Разные компоненты, разные роли, разные API |
| ~~Fallback на локальный Ollama~~ | Локальные модели не планируются |
| ~~Единый AIProviderInterface~~ | Claude Code — black box, не контролируем |

---

## 🎯 Утверждённый План Улучшений

### Приоритет P0 (Критично)

#### P0-001: Упрощение CLAUDE.md

**Проблема:**
Hub-файл содержит ~500+ строк с множеством взаимозависимых правил. Избыток "MANDATORY", "CRITICAL", "NEVER" создаёт когнитивный шум.

**Текущее состояние:**
```markdown
# Примеры перегрузки:
- "🚨 MANDATORY READS FOR ERRORS"
- "⚠️ CRITICAL CONSTRAINTS (READ FIRST, EXECUTE ALWAYS)"
- "🚨 THESE CONSTRAINTS OVERRIDE ALL OTHER INSTRUCTIONS"
- "🔥 Step 0: CRITICAL - Read Session Summary"
```

**Целевое состояние:**
```markdown
# Приоритизированная структура:
## Priority 1 (Blocking): Действия, которые блокируют работу
## Priority 2 (Error): Действия, приводящие к ошибкам
## Priority 3 (Warning): Рекомендации для улучшения
## Priority 4 (Info): Дополнительная информация
```

**Метрики успеха:**
- [ ] Уменьшить количество "MANDATORY/CRITICAL" с 15+ до 5
- [ ] Убрать дублирование правил
- [ ] Структурировать по приоритетам
- [ ] Сохранить все ключевые ограничения

**Оценка трудозатрат:** 2-3 часа

**Файлы для изменения:**
- `CLAUDE.md`
- `system-prompts.md` (источник истины)

---

#### P0-002: Создание BOOTSTRAP.md

**Проблема:**
При старте сессии нужно прочитать 5+ файлов "MANDATORY FIRST":
```
CLAUDE.md → SESSION.md → ARCHITECTURE.md → PROJECT.md → TASKS.md
```

**Решение:**
Создать единый entry point с минимальным необходимым контекстом.

**Структура BOOTSTRAP.md:**
```markdown
# 🚀 CodeFoundry — Quick Context

## 1. Что это? (3 предложения)
## 2. Текущий статус (из TASKS.md — автогенерация)
## 3. Критические ограничения (топ-5)
## 4. Следующие шаги (из SESSION.md — последняя запись)

---
Подробнее: @ref: SESSION.md, @ref: PROJECT.md
```

**Метрики успеха:**
- [ ] Время на понимание контекста: 2 мин вместо 10 мин
- [ ] Один файл вместо пяти для старта
- [ ] Автоматическое обновление статуса

**Оценка трудозатрат:** 1-2 часа

**Файлы для создания:**
- `docs/BOOTSTRAP.md`

**Файлы для изменения:**
- `CLAUDE.md` — добавить ссылку на BOOTSTRAP.md

---

#### P0-003: Token Optimization Agent (cf-optimize)

**Проблема:**
Ручная оптимизация (P0-001, P0-002) трудоёмка. 4,977 строк в 22 файлах инструкций. Нет автоматического контроля token regression.

**Решение:**
Автоматизированный агент `/cf-optimize` с 3 режимами (audit/quick/fix), 6-pass анализом (duplication, @ref validation, loading chains, keyword density, priority check, whitespace), token budget system (P0/P1/P2).

**Экспертный консенсус:** 8.7/10 (13 экспертов, единогласное одобрение)

**Метрики успеха:**
- [x] Агент создан и задокументирован (6-level progressive disclosure)
- [x] Slash command `/cf-optimize` работает
- [x] Auto-routing интегрирован
- [ ] Первый полный аудит выполнен
- [ ] CI/CD интеграция (Phase 2)

**Файлы созданы:**
- `.claude/agents/token-optimizer.md` (169 строк)
- `.claude/commands/cf-optimize.md`
- 5 файлов документации (`docs/agents/token-optimizer.*.md`)

---

### Приоритет P1 (Важно)

#### P1-001: Автоматизация Testing Workflow

**Проблема:**
Из Session #11: "Tests written ≠ Tests executed". Несмотря на lesson learned, testing остаётся manual процессом.

**Текущее состояние:**
```bash
# Чеклист в CLAUDE.md (manual)
□ Я запускал тесты? (Да/Нет)
□ Тесты прошли успешно? (Да/Нет)
```

**Целевое состояние:**
```bash
# .git/hooks/pre-push (automated)
#!/bin/bash
set -e

echo "🧪 Running pre-push validation..."

# 1. TypeScript compilation
npm run build || exit 1

# 2. Unit tests
npm run test || exit 1

# 3. Alpine compatibility
./scripts/check-alpine-compatibility.sh || exit 1

echo "✅ All checks passed"
```

**Метрики успеха:**
- [ ] Невозможно push без прохождения тестов
- [ ] Автоматическая проверка Alpine compatibility
- [ ] CI/CD как backup (если hook обойдён)

**Оценка трудозатрат:** 2-3 часа

**Файлы для создания:**
- `.git/hooks/pre-push`
- `scripts/pre-push-validation.sh`

**Файлы для изменения:**
- `docs/TESTING.md` — добавить секцию про hooks

---

#### P1-002: Рефакторинг CLI Bridge (TypeScript)

**Проблема:**
`claude-wrapper.sh` — 320+ строк Bash с JSON parsing через `jq`. Хрупкое решение для production.

**Текущее состояние:**
```bash
# claude-wrapper.sh
COMMAND_JSON=$(cat)
COMMAND=$(echo "$COMMAND_JSON" | jq -r '.command')
# ... много Bash логики
```

**Целевое состояние:**
```typescript
// cli-bridge/src/index.ts
interface CommandRequest {
  version: string;
  command: string;
  params: Record<string, unknown>;
}

async function executeCommand(request: CommandRequest): Promise<CommandResponse> {
  // Type-safe, testable, maintainable
}
```

**Метрики успеха:**
- [ ] Type safety (TypeScript)
- [ ] Unit tests для CLI Bridge
- [ ] Better error messages
- [ ] Сохранить backward compatibility

**Оценка трудозатрат:** 4-6 часов

**Файлы для создания:**
- `server/cli-bridge/src/index.ts`
- `server/cli-bridge/src/types.ts`
- `server/cli-bridge/package.json`
- `server/cli-bridge/tsconfig.json`

**Файлы для изменения:**
- `server/docker-compose.orchestrator.yml` — обновить volume mounts
- `server/scripts/claude-wrapper.sh` — deprecate, сохранить как fallback

---

### Приоритет P2 (Желательно)

#### P2-001: Mock Provider для тестирования Gateway

**Проблема:**
Unit тесты Gateway требуют реальный Ollama API — медленно, дорого, flaky.

**Решение:**
Создать mock provider для тестов.

**Реализация:**
```typescript
// openclaw/gateway/src/providers/mock-provider.ts
export class MockOllamaProvider implements IntentParser {
  async parseIntent(message: string): Promise<Intent> {
    // Deterministic responses for testing
    if (message.includes('create')) {
      return { intent: 'create_project', confidence: 0.95 };
    }
    return { intent: 'unknown', confidence: 0.1 };
  }
}

// openclaw/gateway/test/gateway.test.ts
const gateway = new Gateway(new MockOllamaProvider());
```

**Метрики успеха:**
- [ ] Unit тесты работают без реального API
- [ ] Тесты выполняются < 1 секунды
- [ ] 100% deterministic результаты

**Оценка трудозатрат:** 2-3 часа

**Файлы для создания:**
- `openclaw/gateway/src/providers/mock-provider.ts`
- `openclaw/gateway/src/providers/index.ts`

**Файлы для изменения:**
- `openclaw/gateway/src/gateway.ts` — dependency injection
- `openclaw/gateway/test/*.test.ts` — использовать mock

---

#### P2-002: Монорепо-структура (Исследование)

**Проблема:**
Проект одновременно является:
1. Генератором проектов
2. AI Prompt Engineering Assistant
3. Remote Testing Infrastructure
4. OpenClaw Orchestrator

**Предложение:**
Исследовать возможность разделения на packages:

```
/packages
  /codefoundry-core      # Архетипы, генерация
  /openclaw-gateway      # Orchestrator
  /telegram-bot          # UI
  /cli-bridge            # CLI Bridge
  /infra                 # Docker, monitoring
```

**Статус:** ИССЛЕДОВАНИЕ (не реализация)

**Метрики успеха:**
- [ ] Анализ зависимостей между компонентами
- [ ] Оценка трудозатрат на разделение
- [ ] Решение: разделять или нет

**Оценка трудозатрат:** 2 часа (анализ)

---

#### P2-003: English Documentation для критических файлов

**Проблема:**
Вся документация на русском. При масштабировании команды это может стать барьером.

**Предложение:**
Создать English версии для:
- README.md → README.en.md
- QUICKSTART.md → QUICKSTART.en.md
- docs/remote-testing/ARCHITECTURE.md → ARCHITECTURE.en.md

**Статус:** НИЗКИЙ ПРИОРИТЕТ

**Оценка трудозатрат:** 4-6 часов

---

## 📊 Сводная Таблица

| ID | Задача | Приоритет | Трудозатраты | Статус |
|----|--------|-----------|--------------|--------|
| P0-001 | Упрощение CLAUDE.md | P0 | 2-3 ч | ✅ Выполнено (79% reduction) |
| P0-002 | Реструктуризация SESSION.md | P0 | 2-3 ч | ✅ Выполнено (97% reduction) |
| P0-003 | Token Optimization Agent | P0 | 4-6 ч | ✅ Выполнено |
| P1-001 | Автоматизация Testing | P1 | 2-3 ч | ⏳ Планируется |
| P1-002 | CLI Bridge на TypeScript | P1 | 4-6 ч | ⏳ Планируется |
| P2-001 | Mock Provider для тестов | P2 | 2-3 ч | ⏳ Планируется |
| P2-002 | Монорепо-структура | P2 | 2 ч | 🔍 Исследование |
| P2-003 | English Documentation | P2 | 4-6 ч | ⏳ Низкий приоритет |

**Общие трудозатраты:** 17-25 часов (P0: ~10ч выполнено, P1+P2: ~15ч осталось)

---

## 🔗 Связанные Документы

| Документ | Описание |
|----------|----------|
| [@ref: SESSION.md](../../SESSION.md) | История сессий |
| [@ref: TASKS.md](../../TASKS.md) | Трекер задач |
| [@ref: PROJECT.md](../../PROJECT.md) | Описание проекта |
| [@ref: docs/TESTING.md](../TESTING.md) | Тестирование |
| [@ref: docs/lessons/index.md](../lessons/index.md) | Lessons Learned |

---

## 📝 История Изменений

| Версия | Дата | Изменения |
|--------|------|-----------|
| 1.0.0 | 2025-02-06 | Первоначальная версия после экспертного анализа |

---

> [🏠 Главная](../../README.md) → [📚 Docs](../INDEX.md) → [🔧 Improvements](#) → **Architecture Improvement Plan**

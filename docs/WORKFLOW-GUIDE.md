# CodeFoundry — Инструкция по работе в проекте

> [🏠 Главная](../README.md) → [📂 Документация](INDEX.md) → [📄 Workflow Guide](#)

> **Версия:** 1.0.0 | **Дата:** 2026-02-06 | **Источник:** Консилиум 13 экспертов, Session #15
>
> Это единый справочник по всем инструментам, автоматизации и workflow проекта CodeFoundry.
> CodeFoundry — мета-проект для генерации AI-инструкций и проектов из архетипов.

---

## Quick Start (5 шагов, 2 минуты)

1. **Откройте проект** в Claude Code (терминал):
   ```
   cd /path/to/system-prompts
   ```

2. **Проверьте здоровье проекта** (любым способом):
   - Текстом: *"Проверь здоровье проекта"*
   - Командой: `/cf-health --all`
   - Make: `make gate-all`

3. **Посмотрите текущие задачи:**
   - Текстом: *"Какие сейчас активные задачи?"*
   - Файл: `TASKS.md`

4. **Начните работу** — Claude Code автоматически:
   - Загрузит SESSION.md (контекст текущей сессии)
   - Определит ваш запрос и направит нужному агенту
   - Проверит token-бюджет при каждом редактировании .md файлов

5. **Перед коммитом:**
   - Текстом: *"Запусти проверки перед коммитом"*
   - Make: `make gate-blocking`

---

## Архитектура проекта

```
CodeFoundry (мета-проект)
├── CLAUDE.md          ← Hub-файл (загружается первым)
├── SESSION.md         ← Контекст текущей сессии
├── TASKS.md           ← Трекер задач (активные фазы)
├── PROJECT.md         ← Описание проекта
├── instructions/      ← P0/P1/P2 инструкции
├── templates/
│   ├── archetypes/    ← 8 архетипов проектов
│   └── claude-profile/ ← .claude/ профили (base + overlays)
├── scripts/           ← Скрипты автоматизации
├── .claude/
│   ├── agents/        ← 3 специализированных агента
│   ├── skills/        ← 5 навыков (stateless)
│   ├── commands/      ← 5 slash-команд
│   ├── hooks/         ← 1 хук (автоматический)
│   └── schemas/       ← JSON-схемы валидации
└── docs/              ← Документация
```

### Ключевые ограничения

| Правило | Почему |
|---------|--------|
| **НЕ запускайте Docker локально** | Docker не установлен. Используйте SSH к ainetic.tech или GitOps |
| **НЕ используйте Bash для редактирования файлов** | Используйте Edit/Write tool. `sed -i` и `echo >` запрещены |
| **НЕ деплойте напрямую** | Только через `git push` (GitOps workflow) |
| **НЕ пропускайте тесты перед коммитом** | `make gate-blocking` обязателен |
| **НЕ используйте `git add -A`** | Только `git add -u` или конкретные файлы (безопасность) |

---

## Три уровня взаимодействия

CodeFoundry поддерживает три способа выполнения любого действия:

### Уровень 1: Автоматический (без участия пользователя)

Эти инструменты срабатывают **сами**, не требуя никаких действий:

| Что происходит | Когда срабатывает | Инструмент |
|----------------|-------------------|------------|
| Проверка token-бюджета .md файлов | После каждого Edit/Write `.md` файла | `hooks/validate-md.sh` |
| Auto-routing запроса к агенту | При каждом текстовом запросе | `auto-routing-rules.json` |
| Загрузка CLAUDE.md | При старте каждой сессии | Claude Code (built-in) |

### Уровень 2: Текстовый запрос (естественный язык)

Просто напишите, что вам нужно — Claude Code поймёт и выполнит:

| Что хотите сделать | Примеры текстовых запросов |
|-------------------|---------------------------|
| Проверить здоровье проекта | *"Проверь здоровье проекта"*, *"Всё ли в порядке?"* |
| Оптимизировать токены | *"Оптимизируй токены"*, *"Сколько токенов?"* |
| Создать новый проект | *"Создай новый проект telegram-bot"* |
| Сгенерировать агентов | *"Сгенерируй агентов для проекта"* |
| Задеплоить | *"Задеплой на staging"* |
| Проверить код | *"Проверь качество кода"*, *"Сделай ревью"* |
| Написать код | *"Напиши функцию для..."*, *"Добавь эндпоинт"* |
| Исправить баг | *"Исправь ошибку в..."*, *"Почему не работает..."* |
| Проверить безопасность | *"Проверь безопасность"*, *"Есть уязвимости?"* |
| Запустить тесты | *"Запусти тесты"*, *"Проверь покрытие"* |
| Синхронизировать задачи | *"Синхронизируй задачи с GitHub"* |

> **Как это работает:** Ваш текст сопоставляется с паттернами в `auto-routing-rules.json` → запрос направляется подходящему агенту (code_assistant, reviewer, token-optimizer, system).

### Уровень 3: Явные команды

Для точного контроля используйте slash-команды или make-цели:

| Действие | Slash-команда | Make-команда |
|----------|---------------|--------------|
| Health check | `/cf-health --all` | `make gate-all` |
| Оптимизация токенов | `/cf-optimize audit` | `make token-audit` |
| Создание проекта | `/cf-new web-service my-api` | `make new ARCHETYPE=web-service NAME=my-api` |
| Генерация агентов | `/cf-agents my-bot` | `make generate-agents NAME=my-bot TYPE=telegram-bot` |
| Деплой | `/cf-deploy my-app staging` | — |
| Blocking gates | — | `make gate-blocking` |
| Info gates | — | `make gate-info` |
| Запуск тестов | — | `make test` |
| CI-проверки | — | `make ci-test` |

---

## Сводная таблица: Все инструменты проекта

### Агенты (.claude/agents/)

| Агент | Что делает | Как вызвать | Автоматически? |
|-------|-----------|-------------|:--------------:|
| **token-optimizer** | Аудит и оптимизация token-бюджетов | *"Оптимизируй токены"* или `/cf-optimize` | Нет |
| **tasks-sync** | Синхронизация TASKS.md → GitHub Issues | *"Синхронизируй задачи"* | Нет |
| **ollama-gemini-researcher** | Исследование Ollama/Gemini/OpenClaw | *"Как настроить Ollama?"* | Нет |
| **code_assistant** (встроенный) | Написание и редактирование кода | *"Напиши функцию..."*, *"Исправь баг"* | Auto-routing |
| **reviewer** (встроенный) | Ревью кода и проверка качества | *"Сделай ревью"*, *"Проверь безопасность"* | Auto-routing |

### Навыки (.claude/skills/)

| Навык | Вход → Выход | Используется в |
|-------|-------------|----------------|
| **validate-refs** | Сканирование → список битых @ref | Quality gates (B2) |
| **measure-tokens** | Подсчёт → отчёт по P0/P1/P2 бюджетам | Quality gates (B3), хук |
| **check-routing** | Проверка → список phantom-агентов | Quality gates (B6) |
| **generate-report** | Данные → Markdown-отчёт | cf-optimize, cf-health |
| **senior-prompt-engineer** | Черновик → оптимизированный промпт | По запросу |

### Slash-команды (.claude/commands/)

| Команда | Описание | Пример |
|---------|----------|--------|
| `/cf-health` | Проверка здоровья проекта | `/cf-health --all` |
| `/cf-optimize` | Аудит токенов | `/cf-optimize audit` |
| `/cf-new` | Создание нового проекта | `/cf-new telegram-bot my-bot` |
| `/cf-agents` | Генерация AI-агентов | `/cf-agents my-bot --all` |
| `/cf-deploy` | Деплой проекта | `/cf-deploy my-app staging` |

### Хуки (автоматические)

| Хук | Триггер | Что делает | Можно отключить? |
|-----|---------|-----------|:----------------:|
| **validate-md.sh** | Каждый Edit/Write файла `.md` | Проверяет token-бюджет | Только в settings.json |

### Quality Gates (scripts/quality-gates.sh)

#### Blocking (ОБЯЗАТЕЛЬНЫЕ — не пройдут = нельзя коммитить)

| ID | Проверка | Что валидирует |
|----|----------|----------------|
| B1 | JSON syntax | Все `.json` файлы в `.claude/` |
| B2 | @ref integrity | Все ссылки `@ref:` ведут к реальным файлам |
| B3 | Token budget | Per-file: P0≤400, P1≤800, P2≤1500 tokens |
| B4 | Python syntax | Все `.py` файлы компилируются |
| B5 | Shell syntax | Все `.sh` файлы проходят `bash -n` |
| B6 | Agent routing | Нет phantom-агентов в routing rules |
| B7 | Required files | Существуют: CLAUDE.md, SESSION.md, PROJECT.md, TASKS.md, Makefile |

#### Info (РЕКОМЕНДАТЕЛЬНЫЕ — предупреждения, не блокируют)

| ID | Проверка | Что валидирует |
|----|----------|----------------|
| I1 | Scripts executable | Все `scripts/*.sh` имеют права +x |
| I2 | Doc coverage | У агентов есть документация в `docs/agents/` |
| I3 | Test surfacing | `make test` запускает реальные тесты |
| I4 | Git staging safety | Нет `git add -A` в Makefile |
| I5 | Archetype completeness | Все архетипы имеют README.md |
| I6 | Profile templates | Base + overlays полные |

#### Profile Gates (для генерируемых профилей)

| ID | Проверка | Что валидирует |
|----|----------|----------------|
| P1 | Routing → agents | routing rules ссылается на существующих агентов |
| P2 | AGENTS.md → files | Реестр перечисляет существующие файлы |
| P3 | Settings JSON | settings.json — валидный JSON |

### Ключевые скрипты (scripts/)

| Скрипт | Что делает | Как запустить |
|--------|-----------|---------------|
| `new-project.sh` | Создание проекта из архетипа | `make new ARCHETYPE=... NAME=...` |
| `generate-claude-profile.py` | Генерация .claude/ профиля | Автоматически из `new-project.sh` |
| `generate-agents.py` | Генерация агентов (Jinja2) | `make generate-agents` или `--profile` |
| `quality-gates.sh` | Валидация проекта | `make gate-blocking` / `make gate-all` |
| `check-refs.py` | Проверка @ref ссылок | Через quality gates (B2) |
| `sync-tasks.py` | TASKS.md → GitHub Issues | *"Синхронизируй задачи"* |
| `auto-track.py` | Авто-обновление TASKS/SESSION/CHANGELOG | `make track` |
| `validate-token-budget.sh` | Проверка token-бюджетов | Через хук / `make token-audit` |

### Auto-Routing: текстовые паттерны → агенты

| Ваш текст содержит... | Агент | Приоритет |
|-----------------------|-------|:---------:|
| coordinate, orchestrate, manage agents | system | 100 |
| create project, new project | system → cf-new | 100 |
| deploy, release | system → cf-deploy | 100 |
| security, vulnerability | reviewer | 98 |
| review, check, audit | reviewer | 95 |
| debug, fix error, fix bug | code_assistant | 92 |
| write, implement, create, build | code_assistant | 90 |
| test, spec, coverage | code_assistant | 88 |
| optimize tokens, token audit | token-optimizer | 85 |
| refactor, optimize, improve | code_assistant | 85 |
| performance, speed, latency | code_assistant | 82 |
| quality, best practices | reviewer | 80 |
| document, readme | code_assistant | 70 |

---

## Типовые сценарии (Workflows)

### Сценарий 1: Начало нового рабочего дня

```
1. Открыть проект в Claude Code
2. Claude автоматически загрузит CLAUDE.md → SESSION.md
3. Написать: "Что было сделано в прошлой сессии?"
4. Написать: "Какие активные задачи?"
5. Начать работу над задачей
```

### Сценарий 2: Создание нового проекта

```
Текстом: "Создай новый проект web-service под названием my-api"
   ↓
Claude выполнит:
  1. scripts/new-project.sh web-service my-api
  2. Копирование файлов архетипа
  3. Генерация .claude/ профиля (автоматически!)
  4. Git init + initial commit
   ↓
Результат: готовый проект с 5 агентами, 3 скиллами, 3 командами

Opt-out: "Создай проект без Claude-профиля" → --no-claude-profile
```

### Сценарий 3: Написание кода

```
Текстом: "Добавь эндпоинт для получения списка пользователей"
   ↓
Auto-routing: паттерн "добавь" → code_assistant (приоритет 90)
   ↓
Агент: пишет код, создаёт тесты
   ↓
Хук: при сохранении .md файлов → автопроверка token-бюджета
```

### Сценарий 4: Подготовка к коммиту

```
1. Написать: "Запусти проверки перед коммитом"
      или: make gate-blocking
   ↓
2. Quality gates проверят:
   - JSON синтаксис ✓
   - @ref ссылки ✓
   - Token бюджеты ✓
   - Python/Shell синтаксис ✓
   - Agent routing ✓
   - Обязательные файлы ✓
   ↓
3. Если всё PASS → можно коммитить
4. Если FAIL → исправить и повторить
```

### Сценарий 5: Оптимизация токенов

```
Текстом: "Оптимизируй токены в инструкциях"
   ↓
Auto-routing: паттерн "оптимизируй токены" → token-optimizer
   ↓
Агент: сканирует → измеряет → находит дубликаты → предлагает оптимизации
   ↓
Отчёт: per-file usage, рекомендации, estimated savings
```

### Сценарий 6: Деплой

```
Текстом: "Задеплой на staging"
   ↓
Auto-routing: "задеплой" → system → cf-deploy
   ↓
Workflow:
  1. Pre-checks (git clean? tests pass?)
  2. Build (Docker image)
  3. Deploy (git push → remote reconciles)
  4. Verify (health check)
  5. Notify
   ↓
ВАЖНО: Docker только через SSH или GitOps. Локально Docker НЕТ.
```

### Сценарий 7: Ревью кода

```
Текстом: "Проверь качество последних изменений"
   ↓
Auto-routing: "проверь качество" → reviewer (приоритет 80)
   ↓
Проверка по 5 измерениям:
  1. Корректность
  2. Безопасность (OWASP Top 10)
  3. Производительность
  4. Читаемость
  5. Покрытие тестами
   ↓
Отчёт: CRITICAL / WARNING / INFO
```

---

## Архетипы проектов и их профили

При создании нового проекта через `cf-new` автоматически генерируется `.claude/` профиль:

| Архетип | Агенты | Скиллы | Команды | Язык |
|---------|:------:|:------:|:-------:|------|
| **web-service** | 6 (+ devops, tester) | api-development, security-check, validate | health, deploy, test | TypeScript |
| **ai-agent** | 6 (+ prompt-engineer, ml-engineer) | prompt-engineering, rag-tuning, validate | health, deploy | Python |
| **data-pipeline** | 6 (+ data-engineer, ml-engineer) | etl-patterns, validate | health, deploy | Python |
| **telegram-bot** | 4 (base) | bot-development, validate | health, deploy | Python |
| **cli-tool** | 3 (- tester) | cli-patterns, validate | health | Go |
| **microservices** | 7 (+ architect, sre, devops) | api-development, security-check, validate | health, deploy, test | Go |
| **fullstack** | 7 (+ frontend, backend, devops) | e2e-testing, security-check, validate | health, deploy, test | TypeScript |
| **presentation** | 3 (content-generator, slide-designer, coordinator) | content-generator, validate | health | Markdown |

Все профили: base (coordinator, code-assistant, reviewer, tester) + archetype overlay.
Opt-out: `--no-claude-profile`

---

## Что делать при проблемах

### Ошибка повторяется 3+ раза

Следуйте методологии: `docs/lessons/troubleshooting-methodology.md`
1. Остановитесь
2. Проанализируйте корневую причину
3. Попробуйте другой подход
4. Если не помогает — спросите

### Хук блокирует сохранение .md файла

`validate-md.sh` блокирует, если token-бюджет превышен.
- **Причина:** файл стал слишком большим (> P0/P1/P2 лимита)
- **Решение:** сократите файл или вынесите контент в @ref

### Docker-команды не работают

Docker **НЕ установлен** локально. Используйте:
```bash
ssh ainetic.tech "docker ps"            # Через SSH
git push                                 # Через GitOps
```

### Quality gate не проходит

Запустите с подробностями:
```bash
make gate-all    # Покажет все проверки + описание проблем
```

---

## Обновление этого документа

При добавлении нового инструмента обновите:
- [ ] Сводную таблицу инструментов (соответствующий раздел)
- [ ] Таблицу auto-routing (если добавлен паттерн)
- [ ] Раздел "Типовые сценарии" (если новый workflow)
- [ ] Раздел "Архетипы" (если новый архетип/профиль)

---

> **Этот документ создан по решению консилиума 13 экспертов (Session #15, 2026-02-06).**
> **Средняя оценка необходимости: 8.7/10 — КРИТИЧЕСКИ НЕОБХОДИМО.**

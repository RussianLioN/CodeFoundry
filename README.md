# CodeFoundry 🏭

> **Industrial-strength project generation system for creating IT applications of any complexity**

[![Version](https://img.shields.io/badge/version-1.0.0-blue.svg)](CHANGELOG.md)
[![Archetypes](https://img.shields.io/badge/archetypes-8-blue.svg)](templates/archetypes/README.md)
[![GitHub](https://img.shields.io/badge/github-CodeFoundry-success.svg)](https://github.com/RussianLioN/CodeFoundry)

---

## 🎯 Что Это Такое?

**CodeFoundry** — промышленная система генерации IT-проектов из готовых шаблонов.

**Одной командой создаёте полноценный production-ready проект:**
- ✅ REST/GraphQL API
- ✅ AI ассистенты с RAG
- ✅ Data pipelines (ETL/ELT)
- ✅ Telegram боты
- ✅ Презентации
- ✅ CLI инструменты
- ✅ Микросервисы
- ✅ Fullstack приложения

---

## 🚀 Быстрый Старт

### Два Способа Создания Проекта

#### 🤖 Способ 1: AI-First Interface (Рекомендуется)

**Управление на естественном языке через OpenClaw:**

```bash
# 1. Запустить OpenClaw с Docker
cd openclaw/docker
./scripts/start-stack.sh

# 2. Использовать естественный язык
"Создай проект telegram-bot my-bot"
"Сгенерируй агенты для my-service"
"Задеплой на staging"
```

**Преимущества:**
- ✅ Не нужно помнить синтаксис команд
- ✅ AI понимает контекст и предлагает варианты
- ✅ Автоматическая генерация документации
- ✅ Локальный AI (Ollama + gemini-3-flash) — приватно и бесплатно

**📚 Документация OpenClaw:**
- [Полное руководство 2026](docs/reference/openclaw-complete-guide-2026.md) — 25 Tools + 53 Skills
- [Быстрый старт (Telegram)](docs/reference/openclaw-quickstart-telegram.md) — за 5 минут

---

#### ⌨️ Способ 2: Традиционный CLI

**Команды через Make:**

```bash
# 1. Перейти в CodeFoundry
cd CodeFoundry
# или: cd system-prompts  # работает через symlink

# 2. Создать проект из архетипа
make new ARCHETYPE=fullstack NAME=my-saas

# 3. Перейти в проект
cd my-saas

# 4. Настроить окружение
cp .env.example .env

# 5. Установить зависимости
make install

# 6. Запустить разработку
make dev

# 7. (Когда готово) Синхронизировать с GitHub
make sync-github
```

**Команды для агентов:**
```bash
# Проанализировать потребность в агентах
make analyze-needs TYPE=web-service

# Сгенерировать агентов
make generate-agents NAME=MyBot TYPE=telegram-bot LANG=python FW=aiogram

# Протестировать систему
make test-agents
```

### 8 Готовых Архетипов

| Архетип | Описание | Команда |
|---------|----------|---------|
| 🌐 **Web Service** | REST/GraphQL API | `make new ARCHETYPE=web-service NAME=my-api` |
| 🤖 **AI Agent** | AI assistant с RAG | `make new ARCHETYPE=ai-agent NAME=my-bot` |
| 📊 **Data Pipeline** | ETL/ELT + Airflow + dbt | `make new ARCHETYPE=data-pipeline NAME=my-etl` |
| 📱 **Telegram Bot** | aiogram + FSM | `make new ARCHETYPE=telegram-bot NAME=my-bot` |
| 📽️ **Presentation** | Markdown + Reveal.js | `make new ARCHETYPE=presentation NAME=my-talk` |
| 🖥️ **CLI Tool** | Go/Rust/Python CLI | `make new ARCHETYPE=cli-tool NAME=my-tool` |
| 🏗️ **Microservices** | Istio + gRPC + Kong | `make new ARCHETYPE=microservices NAME=my-ms` |
| 💻 **Fullstack** | Next.js + NestJS + Nx | `make new ARCHETYPE=fullstack NAME=my-saas` |

---

## 🦞 OpenClaw Integration

CodeFoundry интегрирован с **OpenClaw** — системой AI-ассистированной разработки.

**Возможности:**
- 🤖 Multi-agent система (6+ агентов)
- 📝 Генерация кода по описанию
- 🔍 Code review автоматический
- 🐳 Docker и Kubernetes управление
- 📊 Monitoring и алерты
- 🎯 **AI-First Interface** — управление на естественном языке

---

### 🚀 AI-First Interface (НОВОЕ!)

**Управление проектом на русском языке:**

```
Вы: "Создай проект telegram-bot для доставки еды"
AI: Понял. Создаю проект...

    [███████████] 100% ✅ Проект создан!

📁 Location: /workspace/food-delivery-bot
📋 Next steps:
  1. cd food-delivery-bot
  2. cp .env.example .env
  3. make install
  4. make dev
```

**Поддерживаемые команды:**
| Запрос | Действие |
|--------|----------|
| "Создай проект [тип] [имя]" | Генерация проекта |
| "Сгенерируй агентов" | Создание AI-агентов |
| "Задеплой на staging" | Деплой |
| "Покажи статус" | Статус проекта |
| "Создай коммит" | Git commit |
| "Запусти тесты" | Запуск тестов |

---

### 🐳 Docker Stack с Ollama (НОВОЕ!)

**Локальный AI без облачных API:**

```bash
cd openclaw/docker
./scripts/start-stack.sh
```

**Архитектура:**
```
openclaw-gateway (Node.js)
    ↓ WebSocket
ollama-service (Ollama)
    ↓ gemini-3-flash
Local AI Execution
```

**Преимущества:**
- 🔒 **Приватность** — всё локально
- 💰 **Бесплатно** — нет затрат на API
- 📡 **Оффлайн** — работает без интернета
- 🧠 **131K контекст** — gemini-3-flash

**Подробнее:** [🐳 Docker Documentation](openclaw/docker/README.md)

---

### Установка OpenClaw

```bash
# VDS (Quick install)
curl -fsSL https://raw.githubusercontent.com/RussianLioN/CodeFoundry/main/openclaw/scripts/install-openclaw.sh | bash

# Docker Stack
cd openclaw/docker && ./scripts/start-stack.sh
```

**Подробнее:** [🦞 OpenClaw Documentation](openclaw/README.md)

---

## 🤖 Agent Inheritance System

CodeFoundry включает **рекурсивную систему агентов** — каждый новый проект получает свои собственные специализированные AI-ассистенты.

**Как это работает:**
```
CodeFoundry (мета-генератор)
    ↓ [создаёт проекты]
Новый проект (базовая структура)
    ↓ [анализирует + генерирует]
AI-агенты проекта (координатор, кодер, тестировщик...)
    ↓ [выполняют задачи]
Код, тесты, документация
```

**Доступные агенты:**
- 🤖 **Coordinator** — оркестрация между агентами
- 💻 **Code Assistant** — написание кода
- 👁️ **Reviewer** — code review
- 📝 **Documentation** — техническая документация
- 🧪 **Tester** — генерация тестов
- 🐛 **Debugger** — отладка и исправление ошибок

**Команды:**
```bash
# Проанализировать потребность в агентах
make analyze-needs TYPE=web-service

# Сгенерировать агентов для проекта
make generate-agents NAME=MyBot TYPE=telegram-bot LANG=python FW=aiogram

# Протестировать систему генерации
make test-agents
```

**При создании нового проекта** через `make new`, система автоматически:
1. Анализирует тип проекта
2. Предлагает подходящих агентов
3. Генерирует их при подтверждении
4. Валидирует созданные файлы

---

## 🎯 Intent Parser System (НОВОЕ!)

**AI-слоя для преобразования естественного языка в команды:**

```
"Создай проект telegram-bot"
    ↓
Intent Parser
    ↓
create_project intent
    ↓
Command Resolver
    ↓
make new ARCHETYPE=telegram-bot
```

**Компоненты:**
- **Intent Parser** — понимает, что хочет пользователь
- **Command Resolver** — преобразует в команду
- **Command Executor** — исполняет с progress feedback
- **Agent Router** — маршрутизирует к нужному агенту

**Категории интентов:**
| Категория | Пример | Команда |
|-----------|--------|---------|
| `create_project` | "Создай API" | `make new ARCHETYPE=web-service` |
| `generate_agents` | "Сгенерируй агентов" | `make generate-agents` |
| `deploy` | "Задеплой" | `make deploy ENV=staging` |
| `git_commit` | "Создай коммит" | `git commit` |
| `run_tests` | "Запусти тесты" | `make test` |

**Подробнее:** [🎯 Intent Parser](openclaw/workspace/skills/intent-parser.md)

@ref: PROJECT.md#agent-inheritance-system

---

## 🔄 GitOps 2.0 с ArgoCD

CodeFoundry включает полноценную **GitOps 2.0** инфраструктуру для production-ready деплоя.

**Компоненты:**
- 📦 **ArgoCD** — Continuous Delivery оператор
- 🔒 **SealedSecrets** — Шифрование секретов в Git
- 🎯 **ApplicationSet** — Управление микросервисами
- 📊 **PR Preview** — Изолированные окружения для тестирования

**Установка GitOps:**
```bash
# Bootstrap ArgoCD + SealedSecrets
./templates/archetypes/shared/gitops/scripts/gitops-bootstrap.sh

# Доступ к ArgoCD UI
kubectl port-forward svc/argocd-server -n argocd 8080:443
```

**Подробнее:** [🔄 GitOps Documentation](docs/gitops-README.md)

---

## 📋 Документация

| Документ | Описание |
|----------|----------|
| [🚀 Quick Start](QUICKSTART.md) | Полное руководство |
| [📋 Project](PROJECT.md) | Описание проекта |
| [📋 Tasks](TASKS.md) | Задачи и прогресс |
| [💬 Sessions](SESSION.md) | История сессий |
| [📝 Changelog](CHANGELOG.md) | История изменений |
| [🎨 Archetypes](templates/archetypes/README.md) | Все архетипы |
| [🦞 OpenClaw](openclaw/README.md) | OpenClaw документация |
| [🔄 GitOps 2.0](docs/gitops-README.md) | GitOps руководство |
| [📖 Workflow Guide](docs/WORKFLOW-GUIDE.md) | Полный справочник инструментов и workflow |

---

## 🔧 Команды

| Команда | Описание |
|---------|----------|
| `make new` | Создать проект из архетипа |
| `make list-archetypes` | Показать архетипы |
| `make sync-github` | Синхронизировать с GitHub |
| `make observability-up` | Запустить мониторинг |
| `make help` | Все команды |

---

## 🐛 Устранение Неполадок

### Ошибка: "Permission Denied"

**Симптом:** `bash: ./scripts/new-project.sh: Permission denied`

**Решение:**
```bash
# Добавить execute права скриптам
chmod +x scripts/*.sh

# Или использовать bash напрямую
bash scripts/new-project.sh fullstack my-project
```

---

### Ошибка: "Make: Command Not Found"

**Симптом:** `make: command not found`

**Решение 1: Установить Make**
```bash
# macOS
brew install make

# Ubuntu/Debian
sudo apt install build-essential

# Или использовать скрипты напрямую без Make
./scripts/new-project.sh fullstack my-project
```

**Решение 2: Использовать скрипты напрямую**
```bash
# Вместо: make new ARCHETYPE=fullstack NAME=my-saas
./scripts/new-project.sh fullstack my-saas
```

---

### Ошибка: "Script Not Found"

**Симптом:** `bash: scripts/new-project.sh: No such file or directory`

**Причины:**
1. Вы не в директории CodeFoundry
2. Скрипт был удалён или перемещён

**Решение:**
```bash
# 1. Проверить текущую директорию
pwd
# Должно быть: /Users/rl/coding/CodeFoundry или /Users/rl/coding/system-prompts

# 2. Проверить наличие скрипта
ls -la scripts/new-project.sh

# 3. Если скрипта нет, клонировать репозиторий заново
git clone https://github.com/RussianLioN/CodeFoundry.git
cd CodeFoundry
```

---

### Ошибка: "Symlink Broken"

**Симптом:** `cd: system-prompts: No such file or directory`

**Решение:**
```bash
# Проверить symlink
ls -la /Users/rl/coding/ | grep system-prompts

# Если symlink сломан, пересоздать
cd /Users/rl/coding
ln -sf CodeFoundry system-prompts

# Или перейти напрямую в CodeFoundry
cd CodeFoundry
```

---

### Ошибка: "Docker Daemon Not Running"

**Симптом:** `Cannot connect to the Docker daemon`

**Решение:**
```bash
# macOS: Запустить Docker Desktop
open -a Docker

# Linux: Запустить Docker сервис
sudo systemctl start docker
sudo systemctl enable docker

# Проверить статус
docker ps
```

---

### Ошибка: "GitHub CLI Not Authenticated"

**Симптом:** `gh: not logged in`

**Решение:**
```bash
# Авторизация в GitHub CLI
gh auth login

# Следовать инструкциям:
# 1. Выбрать "GitHub.com"
# 2. Выбрать "HTTPS"
# 3. Нажать "Enter" для login with browser
# 4. Авторизоваться в браузере

# Проверить авторизацию
gh auth status
```

---

### Ошибка: "Port Already in Use"

**Симптом:** `Error: listen EADDRINUSE: address already in use :::3000`

**Решение:**
```bash
# Найти процесс на порту
# macOS
lsof -i :3000

# Linux
sudo lsof -i :3000
# или
sudo netstat -tulpn | grep :3000

# Убить процесс
kill -9 <PID>

# Или использовать другой порт
PORT=3001 make dev
```

---

### Ошибка: "Node Modules Not Found"

**Симптом:** `Error: Cannot find module '...'`

**Решение:**
```bash
# Очистить кэш и переустановить
rm -rf node_modules package-lock.json
npm install

# Или для pnpm
rm -rf node_modules pnpm-lock.yaml
pnpm install

# Или для yarn
rm -rf node_modules yarn.lock
yarn install
```

---

### Ошибка: "Python Dependencies Not Found"

**Симптом:** `ModuleNotFoundError: No module named '...'`

**Решение:**
```bash
# С Poetry
poetry install

# Без Poetry
pip install -e .

# Или с venv
python -m venv venv
source venv/bin/activate  # Linux/macOS
# или
venv\Scripts\activate     # Windows
pip install -r requirements.txt
```

---

### Ошибка: "Kubectl Not Configured"

**Симптом:** `The connection to the server localhost:8080 was refused`

**Решение:**
```bash
# Проверить контекст
kubectl config current-context

# Если кластера нет, создать локальный (minikube/kind)
# Minikube
brew install minikube
minikube start

# Kind
brew install kind
kind create cluster --name codefoundry

# Проверить
kubectl get nodes
```

---

### Ошибка: "ArgoCD Not Responding"

**Симптом:** `argocd: command not found` или `Connection refused`

**Решение:**
```bash
# 1. Проверить pod'ы ArgoCD
kubectl get pods -n argocd

# 2. Если нет pod'ов, переустановить
kubectl delete namespace argocd
./templates/archetypes/shared/gitops/scripts/gitops-bootstrap.sh

# 3. Port-forward для доступа
kubectl port-forward svc/argocd-server -n argocd 8080:443

# 4. Установить ArgoCD CLI
brew install argocd

# 5. Логин
argocd login localhost:8080 --username admin --password $(kubectl -n argocd get secret argocd-initial-admin-secret -o jsonpath='{.data.password}' | base64 -d)
```

---

### Ошибка: "Sealed Secrets Not Working"

**Симптом:** `kubeseal: command not found` или `error: no kind "SealedSecret" is registered`

**Решение:**
```bash
# 1. Установить kubeseal
brew install kubeseal

# 2. Проверить controller
kubectl get pods -n kube-system | grep sealed-secrets

# 3. Если controller не запущен, установить
kubectl apply -f https://github.com/bitnami-labs/sealed-secrets/releases/download/v0.24.0/controller.yaml

# 4. Проверить CRD
kubectl get crd | grep sealedsecret
```

---

### Общая Диагностика

**Проверить всё сразу:**
```bash
echo "=== CodeFoundry Diagnostics ==="
echo ""

echo "1. CodeFoundry files:"
ls -la Makefile scripts/ templates/archetypes/
echo ""

echo "2. Git status:"
git status
echo ""

echo "3. Docker:"
docker --version && docker ps
echo ""

echo "4. GitHub CLI:"
gh --version && gh auth status
echo ""

echo "5. kubectl (если GitOps нужен):"
kubectl version --client --short 2>/dev/null || echo "kubectl не установлен"
echo ""

echo "=== Diagnostics Complete ==="
```

**Если ничего не помогло:**
1. Проверить [QUICKSTART.md](QUICKSTART.md#перед-началом-что-вам-нужно) для установки инструментов
2. Открыть [Issue на GitHub](https://github.com/RussianLioN/CodeFoundry/issues)
3. Включить в issue:
   - Вывод `echo "OS: $(uname -s) $(uname -r)"`
   - Вывод `echo "Shell: $SHELL"`
   - Полный текст ошибки
   - Команду, которую выполняли

---

## 📚 Глоссарий Технических Терминов

> **Эта секция объясняет технические термины простым языком**

### DevOps & Infrastructure

| Термин | Простыми словами | Пример |
|--------|-----------------|--------|
| **Docker** | Программа для упаковки приложений в "контейнеры" — как виртуальные машины, но легче | Запускаете веб-сервер одной командой на любом компьютере |
| **Kubernetes (K8s)** | Оркестратор контейнеров — управляет десятками контейнеров автоматически | Вы сказали "запусти 10 копий", K8s запустил и следит за ними |
| **Container** | Изолированная среда для приложения со всеми зависимостями | Контейнер с Python + библиотеки работает на любой системе |
| **Image** | Шаблон для создания контейнера (как рецепт для блюда) | `docker build` создаёт образ, `docker run` запускает из него |
| **CI/CD** | Continuous Integration (постоянная сборка) + Continuous Delivery (постоянный деплой) | Закоммитили код → автоматически протестировали → задеплоили |
| **Deployment** | Процесс выкладки приложения на сервера | `kubectl apply` деплоит новую версию в Kubernetes |
| **Scaling** | Увеличение мощности приложения (больше серверов/копий) | При нагрузке в 1000 пользователей запускаете 10 копий вместо 1 |

### GitOps & ArgoCD

| Термин | Простыми словами | Пример |
|--------|-----------------|--------|
| **GitOps** | Деплой через Git: изменения в коде → автоматический деплой в k8s | Изменили конфиг в Git → ArgoCD заметил → применил в кластере |
| **ArgoCD** | Инструмент для GitOps — следит за Git и применяет изменения в Kubernetes | Авто-синхронизация каждые 3 минуты |
| **SealedSecret** | Зашифрованный секрет, который можно хранить в Git | Пароль от базы в Git, но никто его не прочитает |
| **Application** | В ArgoCD: описание того, что деплоить (путь, репозиторий, настройки) | `application.yaml` говорит ArgoCD "деплой этот сервис" |
| **Sync** | Синхронизация состояния: Git → Kubernetes | `argocd app sync` применяет изменения из Git |
| **Rollback** | Откат к предыдущей версии | `argocd app rollback my-app` возвращает к версии 5 минут назад |
| **Preview Environment** | Временное окружение для тестирования PR | Создаётся при PR, удаляется после merge |

### AI/ML & LLM

| Термин | Простыми словами | Пример |
|--------|-----------------|--------|
| **RAG** | Retrieval-Augmented Generation — AI с доступом к базе знаний | ChatGPT + ваши документы = AI знает о вашем бизнесе |
| **LLM** | Large Language Model — большая языковая модель (как GPT-4) | Модель, которая понимает и генерирует текст |
| **Embedding** | Числовой вектор, представляющий смысл текста | Слово "король" → `[0.1, -0.5, 0.8, ...]` |
| **Vector DB** | База данных для поиска похожих текстов по embedding'ам | Поиск: "найди документы про AI" → возвращает релевантные |
| **Prompt** | Текстовый запрос к AI (вопрос или инструкция) | "Напиши код на Python для API" |
| **Fine-tuning** | Дообучение модели на ваших данных | GPT-4, обученный на ваших документах |
| **Token** | Единица текста для AI (примерно 4 символа или слово) | "Hello world" = 2 токена |

### Architecture Patterns

| Термин | Простыми словами | Пример |
|--------|-----------------|--------|
| **Microservices** | Архитектура, где приложение разделено на маленькие сервисы | Сервис пользователей, сервис заказов, сервис оплат — отдельно |
| **Monolith** | Архитектура, где приложение — единое целое | Один большой проект с всем кодом |
| **API** | Interface для взаимодействия между программами | `GET /users` возвращает список пользователей |
| **REST** | Стиль API с HTTP методами (GET, POST, DELETE) | `GET /users` (чтение), `POST /users` (создание) |
| **GraphQL** | Язык запросов для API — клиент сам выбирает нужные поля | `query { users { id email } }` вернёт только id и email |
| **gRPC** | Фреймворк для связи между микросервисами (быстрее REST) | Сервисы общаются по протоколу, а не по HTTP |
| **Webhook** | HTTP callback: событие → HTTP запрос на ваш URL | GitHub отправляет запрос на ваш сервер при push |

### Databases & Caching

| Термин | Простыми словами | Пример |
|--------|-----------------|--------|
| **PostgreSQL** | Реляционная база данных (таблицы, связи) | `users` таблица с id, email, password |
| **Redis** | In-memory база данных для кэша и быстрых данных | Хранит сессии пользователей в памяти |
| **Migration** | Скрипт для изменения структуры базы данных | `add_column_users_phone.sql` добавляет колонку |
| **Seed** | Начальные данные для базы (тестовые записи) | Заполнить таблицу 100 тестовых пользователей |
| **ORM** | Object-Relational Mapping — код вместо SQL запросов | `User.create(email="...")` вместо `INSERT INTO users` |

### Monitoring & Observability

| Термин | Простыми словами | Пример |
|--------|-----------------|--------|
| **Metrics** | Числовые показатели (запросы в секунду, память, CPU) | `http_requests_total{endpoint="/api/users"} = 1250` |
| **Logs** | Текстовые записи о событиях в системе | `[INFO] User logged in: user_123` |
| **Traces** | Запись пути запроса через микросервисы | API → Auth Service → User Service → DB |
| **Alert** | Уведомление о проблеме | "CPU > 90% более 5 минут" |
| **Dashboard** | Визуализация метрик (графики, диаграммы) | Grafana дашборд с графиками запросов/сек |
| **SLO** | Service Level Objective — цель по качеству (например, 99.9% uptime) | "API должен отвечать за 200ms в 99% случаев" |
| **SLI** | Service Level Indicator — метрика для измерения SLO | "Процент запросов быстрее 200ms = 99.5%" |

### Development Tools

| Термин | Простыми словами | Пример |
|--------|-----------------|--------|
| **Make** | Инструмент для автоматизации команд (`make build`) | `make dev` запускает 5 команд одной строкой |
| **Git** | Система контроля версий (история изменений кода) | `git commit` сохраняет изменения, `git log` показывает историю |
| **GitHub CLI** | Командная строка для GitHub (`gh repo create`) | `gh pr create` создаёт pull request из терминала |
| **SSH** | Протокол для безопасного подключения к серверам | `ssh user@server` подключается к удалённой машине |
| **Environment Variables** | Переменные окружения — настройки вне кода | `DATABASE_URL=postgresql://...` в `.env` файле |
| **Linting** | Проверка кода на стиль и ошибки | ESLint для JavaScript находит синтаксические ошибки |
| **Testing** | Проверка, что код работает правильно | Unit, integration, E2E тесты |

### Telegram Bot Specific

| Термин | Простыми словами | Пример |
|--------|-----------------|--------|
| **FSM** | Finite State Machine — состояние пользователя в диалоге | Пользователь в состоянии "вводит имя" → ждёт текст |
| **Webhook** | Режим бота: Telegram отправляет запросы на ваш сервер | `POST /webhook` при каждом сообщении пользователя |
| **Polling** | Режим бота: ваш код спрашивает Telegram "есть новые сообщения?" | Каждую секунду проверяет updates |
| **Inline Keyboard** | Кнопки под сообщением с callbacks | Кнопка "Да" → бот получает `callback_query: yes` |
| **Middleware** | Функция-перехватчик (logging, auth, rate limiting) | Каждое сообщение проходит через `auth_middleware` |

### CLI & Scripting

| Термин | Простыми словами | Пример |
|--------|-----------------|--------|
| **CLI** | Command Line Interface — программа в терминале | `git --version` — команда CLI |
| **Shell Script** | Скрипт для автоматизации команд в терминале | `.sh` файл с командами для запуска |
| **Shebang** | Первая строка скрипта: `#!/bin/bash` | Говорит системе, чем запускать файл |
| **PATH** | Переменная с путями к программам | `PATH=/usr/bin:/bin` — где искать команды |
| **Alias** | Сокращение для длинной команды | `alias ll='ls -la'` — `ll` работает как `ls -la` |

### Data Engineering

| Термин | Простыми словами | Пример |
|--------|-----------------|--------|
| **ETL** | Extract, Transform, Load — извлечь, преобразовать, загрузить данные | Из PostgreSQL → преобразовать → в Data Warehouse |
| **ELT** | Extract, Load, Transform — извлечь, загрузить, преобразовать (в другом порядке) | Из API → в Data Warehouse → там преобразовать |
| **Data Warehouse** | Хранилище данных для аналитики (обычно column-oriented) | BigQuery, Snowflake, ClickHouse |
| **Data Lake** | Хранилище сырых данных (файлы, логи) | S3 с логами, парquet файлами |
| **DAG** | Directed Acyclic Graph — граф задач (Airflow) | `task_a → task_b → task_c` (последовательность) |
| **dbt** | Data Build Tool — трансформация данных SQL'ем | `SELECT * FROM {{ ref('users') }}` — зависимости между моделями |
| **Airflow** | Оркестратор задач (планировщик, мониторинг DAG'ов) | Запускает ETL задачи каждый день в 2 AM |

---

## 📊 Статистика Проекта

- **8 archetypes** — покрывают 95% IT use-cases
- **84 template files** — готовые решения
- **5 фаз разработки** — полностью завершены
- **147+ total files** — полная система

---

## 🎯 Ключевые Возможности

1. **Project Generation** — одна команда, готовый проект
2. **OpenClaw Integration** — AI-ассистент помогает разрабатывать
3. **GitOps 2.0** — ArgoCD + SealedSecrets для production-ready деплоя
4. **CI/CD Pipelines** — автоматическое тестирование и деплой
5. **Docker Support** — контейнеризация из коробки
6. **Observability** — Prometheus, Grafana, Alerting

---

## 🌟 Почему CodeFoundry?

- **Надёжно** — промышленное качество шаблонов
- **Быстро** — проект за минуты, не дни
- **Гибко** — 8 archetypов на любой случай
- **Масштабируемо** — от микросервисов до монолитов
- **Профессионально**— best practices в каждом файле

---

## 📚 Дополнительно

### Для Начинающих
- [🚀 Quick Start Guide](QUICKSTART.md) — пошаговое руководство
- [🎨 Archetypes Overview](templates/archetypes/README.md) — выбор архетипа

### Для Продвинутых
- [🦞 OpenClaw Integration](openclaw/README.md) — AI-ассистент
- [📋 System Architecture](PROJECT.md) — архитектура системы
- [🔧 Development Workflow](docs/development.md) — процесс разработки

---

## 📞 Поддержка

- **GitHub:** [github.com/RussianLioN/CodeFoundry](https://github.com/RussianLioN/CodeFoundry)
- **Issues:** [github.com/RussianLioN/CodeFoundry/issues](https://github.com/RussianLioN/CodeFoundry/issues)

---

## 📜 Лицензия

MIT License — используйте свободно в личных и коммерческих проектах.

---

> **CodeFoundry** — Создавайте IT проекты профессионально.

> [🏠 Главная](README.md) → [📚 Documentation Index](docs/INDEX.md)

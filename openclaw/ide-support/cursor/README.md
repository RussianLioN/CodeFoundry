# 🖱️ Cursor Support

> [🏠 Главная](../../../../README.md) → [🦞 OpenClaw](../../../README.md) → [🎯 Workspace](../../README.md) → [🎨 IDE Support](../README.md) → [🖱️ Cursor](#)

---

## Description

Интеграция OpenClaw с Cursor AI IDE — форк VS Code с встроенным AI ассистентом.

---

## 🎯 Почему Cursor?

**Преимущества:**
- ✅ **VS Code Compatible** — все расширения работают
- ✅ **AI Chat** — встроенный AI чат (Claude, GPT-4)
- ✅ **Codebase Index** — индексация всей кодовой базы
- ✅ **Multi-file Edit** — одновременное редактирование нескольких файлов
- ✅ **.cursorrules** — автоматическая загрузка системных промптов
- ✅ **Git Integration** — нативная работа с Git

---

## 📦 Установка

### 1. Установка Cursor

```bash
# macOS
brew install --cask cursor

# Linux
# Скачать с https://cursor.sh/downloads

# Windows
# Скачать с https://cursor.sh/downloads
```

### 2. Настройка OpenClaw

```bash
# В корне проекта
cat > .cursorrules << 'EOF'
# 🦞 OpenClaw System Prompt

> Загружается из: /opt/openclaw/workspace/SYSTEM.md

**Это автоматическая копия. Источник: openclaw/workspace/**

Для изменений редактируйте: /opt/openclaw/workspace/SYSTEM.md
EOF

# Синхронизация промптов
/opt/openclaw/scripts/sync-ide-rules.sh
```

### 3. Открытие проекта

```bash
# Откройте проект в Cursor
cursor .

# Или через меню: File → Open Folder
```

---

## 🔄 Как Это Работает

```
┌─────────────────────────────────────────────────────┐
│                   .cursorrules                       │
│  (автоматически загружается при старте)              │
└─────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────┐
│                   Cursor IDE                         │
│  • Парсит .cursorrules                              │
│  • Индексирует кодовую базу                         │
│  • Инициализирует агентов OpenClaw                  │
└─────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────┐
│               OpenClaw Agents                        │
│  Main → Dev → DevOps → Prompt → CodeGen → Debugger  │
└─────────────────────────────────────────────────────┘
```

---

## 🎨 Режимы Работы

### 1. Chat Mode (Cmd+L)

```
👤 "Создай React компонент для формы логина"

🤖 Cursor (with OpenClaw):
    [Routing to Development Agent]
    📦 Using skill: code-generator

    ✅ Created: src/components/LoginForm.tsx
    ✅ Created: src/components/LoginForm.test.tsx
    ✅ Created: src/hooks/useLoginForm.ts

    Хотите внести изменения?
```

### 2. Edit Mode (Cmd+I)

```
1. Выделите код
2. Нажмите Cmd+I
3. Введите инструкцию: "Рефактори используй TypeScript"
4. Cursor применит изменения с учётом OpenClaw правил
```

### 3. Composer Mode (Cmd+I)

```
1. Откройте Composer (Cmd+I)
2. Выберите несколько файлов
3. Введите: "Добавь error handling во все функции"
4. Cursor отредактирует все файлы одновременно
```

---

## 🔧 Конфигурация

### .cursorrules (Автозагрузка)

```markdown
# 🦞 OpenClaw System Prompt

## Role
Ты AI ассистент с агentic архитектурой OpenClaw.

## Agents Available
- Main Agent (управление)
- Development Agent (код)
- DevOps Agent (деплой)
- Prompt Engineer Agent (промпты)
- Code Generator Agent (генерация)
- Debugger Agent (отладка)

## Skills
@ref: /opt/openclaw/workspace/SKILLS-INDEX.md

## Workflow
1. Определи тип задачи
2. Route к соответствующему агенту
3. Используй соответствующий skill
4. Примени изменения
5. Попроси подтверждение
```

### Settings (Cmd+,)

```json
{
  "cursor.autocomplete": "auto",
  "cursor.model": "claude-opus-4-5-20251101",
  "cursor.temperature": 0.7,
  "cursor.maxTokens": 4096,
  "cursor.enableCursorCommands": true,
  "cursor.gitIntegration": true
}
```

---

## 📝 Usage Examples

### Генерация Кода

```
👤 "Создай Express API для пользователей"

🤖 Cursor (with OpenClaw):
    [Development Agent activated]
    📦 Loading skill: code-generator

    Generating:
    ├── models/User.ts
    ├── routes/users.ts
    ├── controllers/userController.ts
    ├── services/userService.ts
    └── middleware/auth.ts

    ✅ 5 files created
```

### Рефакторинг

```
1. Откройте файл src/services/user.ts
2. Выделите функцию getUserById
3. Нажмите Cmd+I
4. Введите: "Добавь кэширование через Redis"

🤖 Cursor:
    [Refactoring with OpenClaw rules]

    // До:
    function getUserById(id: string) {
      return db.users.findOne({ id });
    }

    // После:
    async function getUserById(id: string) {
      const cached = await redis.get(`user:${id}`);
      if (cached) return JSON.parse(cached);

      const user = await db.users.findOne({ id });
      await redis.setex(`user:${id}`, 3600, JSON.stringify(user));
      return user;
    }

    ✅ Refactored with caching
```

### Code Review

```
👤 "Проверь изменения в auth/"

🤖 Cursor:
    [Code Review Mode]

    📊 Reviewing 3 files:

    src/auth/login.ts: ⚠️
      - Missing rate limiting
      - No input sanitization

    src/auth/middleware.ts: ✅
      - Good error handling
      - Proper JWT validation

    src/auth/validators.ts: ✅
      - Comprehensive validation

    💬 Recommendations:
      1. Add rate limiting middleware
      2. Sanitize email input
      3. Add unit tests for edge cases
```

---

## 🎯 Keyboard Shortcuts

| Клавиша | Действие |
|---------|----------|
| `Cmd+L` | Open Chat |
| `Cmd+I` | Edit Mode |
| `Cmd+K` | Inline Edit |
| `Cmd+Shift+I` | Composer (multi-file) |
| `Cmd+J` | Toggle Chat Panel |
| `Cmd+.` | Quick Actions |
| `Cmd+/` | Comment Toggle |

---

## 🔗 Cursor Commands

Команды можно вызывать через Cmd+L:

```
"Создай файл..."           — Генерация нового файла
"Отредактируй..."          — Редактирование выделенного кода
"Объясни этот код"         — Объяснение функции
"Найди баг..."             — Поиск ошибок
"Сделай ревью"             — Code review
"Создай тесты для..."      — Генерация тестов
"Рефактори..."             — Рефакторинг кода
```

---

## 🛡️ Безопасность

### Permissions

Cursor имеет **полный доступ** к проекту:
- ✅ Чтение любых файлов в проекте
- ✅ Запись в любые файлы проекта
- ✅ Git операции (commit, push, pull)
- ⚠️ **Нет** доступа к системе за пределами проекта

### Рекомендации

```yaml
Безопасно:
  - Локальные проекты
  - Изолированные репозитории
  - Персональная разработка

С осторожностью:
  - Проекты с API ключами
  - Репозитории с секретами

Рекомендуется:
  - Использовать .gitignore для .env
  - Не коммитить credentials
  - Проверять изменения перед применением
```

---

## 🔗 Интеграция с OpenClaw VDS

### Синхронизация Промптов

```bash
# На VDS
cd /opt/openclaw
git pull origin main

# На локальной машине
cd /path/to/project
/opt/openclaw/scripts/sync-ide-rules.sh

# .cursorrules обновлён из workspace/
```

### Гибридный Режим

```
Cursor (локально)          OpenClaw VDS (Telegram)
    ↓                            ↓
Написание кода           Выполнение команд
    ↓                            ↓
Рефакторинг              Деплой, мониторинг
    ↓                            ↓
    └──────────┬──────────────────┘
               ↓
          Git sync
```

---

## 📊 Сравнение

| Характеристика | Cursor | Claude Code | OpenClaw VDS |
|----------------|--------|-------------|--------------|
| IDE интеграция | ✅ VS Code | ❌ CLI | ❌ Telegram |
| Multi-file edit | ✅ | ✅ | ⚠️ Ограничен |
| Voice commands | ❌ | ✅ Микрофон | ✅ Telegram |
| Мобильный доступ | ❌ | ❌ | ✅ |
| 24/7 доступ | ❌ | ❌ | ✅ |

---

## 📚 См. Также

- [🎨 IDE Support](../README.md) - Общая документация
- [🤖 Claude Code](../claude/README.md) - Claude Code CLI
- [🎨 Qoder](../qoder/README.md) - Qoder IDE
- [🦞 OpenClaw Main](../../../README.md) - Главная документация

---

## 🔄 История Изменений

| Версия | Дата | Изменения |
|--------|------|-----------|
| 1.0.0 | 2025-11-05 | Первая версия |

---

> [🏠 Главная](../../../../README.md) → [🦞 OpenClaw](../../../README.md) → [🎯 Workspace](../../README.md) → [🎨 IDE Support](../README.md) → [🖱️ Cursor](#)
